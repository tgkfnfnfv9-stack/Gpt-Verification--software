# ChatGPT Work用：GitHub書き込み依頼書

## 依頼内容

GitHubリポジトリ **`tgkfnfnfv9-stack/Gpt-Verification--software`** に、GPTトレード戦略検証用の汎用HTMLビューアを公開できる状態で書き込んでください。

対象ブランチは **`main`** です。

## 最初に行う確認

1. 対象リポジトリが `tgkfnfnfv9-stack/Gpt-Verification--software` であることを確認してください。
2. 現在のリポジトリ内容を読み取り、既存ファイルがある場合は内容を確認してください。
3. 無関係な既存ファイルを削除・上書きしないでください。
4. APIキー、パスワード、アクセストークン等の秘密情報がファイル内に含まれていないことを確認してください。

## 書き込むファイル

手元のプロジェクトフォルダにある次のファイルを使用してください。

```text
index.html
report.html
README.md
GPT出力JSON_仕様.md
GPT用_出力指示.md
プロジェクト指示書.md
Work_GitHub書き込み依頼書.md
```

サンプルJSONは次の場所へ配置してください。

```text
samples/GPT出力データ_サンプル.json
```

`index.html` はGitHub Pagesのトップページです。`report.html` と同じ汎用検証ビューアを表示します。

## このHTMLの目的

このページは、ChatGPT / 検証エンジンが出力したJSONをユーザーが読み込み、以下をブラウザで確認するためのものです。

- 複数銘柄
- 複数時間足
- 複数インジケータ
- ローソク足
- BUY / SELL
- Entry / Exit
- SL / TP
- トレード一覧
- 勝率
- 期待値
- PF
- 最大DD
- 累積R

初期状態ではデモ相場を表示せず、JSONを読み込んだ場合だけ表示します。

複数JSONを追加でき、読み込み済みデータは**1件ずつ削除**または**すべて削除**できます。

## UI要件

- UIは原則日本語のまま維持してください。
- 銘柄名、RSI、MACD、ATRなど必要な固有名詞以外は不要に英語化しないでください。
- 現在のチャートUIとJSON読み込み機能を壊さないでください。
- PCとスマートフォンの両方で閲覧できる静的HTMLとして維持してください。
- サーバー側プログラムを必須にしないでください。

## GitHubへの書き込み

可能であれば、以下のコミットメッセージで `main` へ反映してください。

```text
Add universal GPT backtest viewer and project docs
```

既存内容との衝突や重要な差分がある場合は、勝手に破壊的変更をせず、内容を報告してください。

## GitHub Pages

ファイルの書き込み後、GitHub Pagesが未設定なら、可能な操作範囲で以下を設定してください。

```text
Settings
→ Pages
→ Source: Deploy from a branch
→ Branch: main
→ Folder: /(root)
→ Save
```

GitHub接続ツールからPages設定まで変更できない場合は、**ファイル書き込みまで実施し、その後ユーザーが行うべき設定を明確に報告**してください。

## 公開確認

GitHub Pagesが有効になった場合は、公開URLを確認し、次を点検してください。

1. `index.html` がトップページとして開く。
2. 日本語UIが文字化けしていない。
3. JSONファイル選択ボタンが表示される。
4. 複数JSONを読み込める。
5. 1件ずつ削除できる。
6. すべて削除できる。
7. チャート、インジケータ、トレード一覧が表示できる。
8. スマートフォン表示で致命的な崩れがない。

## 禁止事項

次をGitHubへ書き込まないでください。

- OpenAI API Key
- GitHub Token
- MT5ログイン情報
- ブローカーの認証情報
- `.env` の秘密値
- 個人情報を含むログ

## 完了報告

最後に以下を簡潔に報告してください。

```text
対象リポジトリ：
対象ブランチ：
追加・更新したファイル：
コミット：
GitHub Pages設定状況：
公開URL：
残っている作業：
```
