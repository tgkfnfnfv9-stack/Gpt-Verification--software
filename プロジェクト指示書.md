# GPTトレード戦略検証プロジェクト 指示書

## 1. プロジェクトの目的

このプロジェクトは、**ChatGPTを戦略研究の中心に置き、過去検証結果を汎用HTMLビューアで確認し、戦略完成後にMT5のリアルタイム監視とGPT裁量へ接続する**ことを目的とする。

最終形は以下。

```text
【戦略研究・過去検証】
ユーザー
  ↓
ChatGPTと戦略を練る
  ↓
銘柄・時間足・期間・戦略を確定
  ↓
過去データで検証
  ↓
検証結果JSONを生成
  ↓
汎用HTMLビューアで確認
  ├─ ローソク足
  ├─ インジケータ
  ├─ 買い / 売り
  ├─ エントリー / 決済
  ├─ 損切り / 利確
  ├─ 期待値
  ├─ PF
  ├─ 勝率
  ├─ 最大DD
  └─ トレード一覧
  ↓
チャートを見て戦略を修正
  ↺
  ↓
戦略完成

【実運用】
完成Strategy
  ↓
ChatGPTがMT5監視プログラムを作成
  ↓
MT5が24時間監視
  ↓
戦略条件成立
  ↓
GPTへ現在までの相場情報を渡す
  ↓
GPT最終裁量
  ├─ BUY
  ├─ SELL
  └─ HOLD
  ↓
固定リスク管理
  ↓
MT5発注
```

---

## 2. 現在のHTMLビューアの役割

HTMLビューアは**戦略を作る場所ではなく、GPTが生成した検証結果を見る場所**とする。

HTML画面に銘柄・期間・時間足などを手入力する機能は持たせない。

### HTMLビューアで行うこと

- GPT検証結果JSONの読み込み
- 1個または複数JSONの追加読み込み
- 複数チャート / 複数銘柄の切り替え
- 読み込んだデータを1件ずつ消去
- 読み込んだデータを一括消去
- ローソク足の確認
- インジケータの確認
- 買い / 売り / 決済位置の確認
- 個別トレードへの移動
- 期待値などの成績確認

### HTMLビューアで行わないこと

- 戦略条件の編集
- 銘柄入力
- 期間入力
- 時間足入力
- APIキー入力
- MT5発注
- GPT APIによるリアルタイム裁量

---

## 3. UI方針

### 基本方針

- UI表示は原則として日本語
- 英語は銘柄名、API名、インジケータ名など必要なものだけ残す
- 初期状態はデータなし
- デモ用の相場データをHTMLへ埋め込まない
- JSONを読み込んだ時だけデータを表示する
- PC / スマートフォンのブラウザで確認しやすくする

### 必須操作

```text
[JSONを追加]

読み込み済みデータ
├─ GOLD / M5 / 戦略A      [このデータを消す]
├─ USDJPY / M15 / 戦略B   [このデータを消す]
└─ BTCUSD / H1 / 戦略C    [このデータを消す]

[すべて消す]
```

1つのJSONだけ読み込む使い方にも対応する。

---

## 4. GPTへ検証を依頼するときの基本入力

ユーザーはChatGPTへ主に以下を伝える。

```text
銘柄：例 XAUUSD
時間足：例 M5
期間：例 2025-01-01 ～ 2025-12-31
戦略アイデア：例 レンジ下抜け後の急速復帰を買う
```

必要に応じて、以下も相談しながら決める。

- BUY条件
- SELL条件
- レンジ定義
- トレンド定義
- インジケータ
- SL
- TP
- 最大保有時間
- セッション
- ボラティリティ条件
- 上位足条件
- スプレッド / 手数料条件

---

## 5. GPTが生成する検証データ

GPT / 検証エンジンは、HTMLビューアが読めるJSONを生成する。

詳細仕様は `GPT出力JSON_仕様.md` を参照。

JSONには少なくとも次を含める。

- 検証名 / 戦略名
- 銘柄
- 時間足
- 検証期間
- OHLC
- 使用インジケータ
- トレード一覧
- BUY / SELL
- Entry / Exit
- SL / TP
- 各トレード損益
- 戦略条件の概要

HTML側ではトレード結果から以下を再計算できること。

- トレード数
- 勝率
- 期待値
- Profit Factor（PF）
- 最大ドローダウン
- 累積R
- BUY成績
- SELL成績

---

## 6. 検証の品質ルール

戦略研究では、見た目だけでなく**再現可能なルール**にする。

### 必須ルール

1. Entry判定に未来のローソク足を使わない。
2. 条件成立時点より後の高値・安値・決済結果をEntry判定へ渡さない。
3. 同じ入力なら同じ固定条件バックテスト結果を再現できるようにする。
4. 手数料、スプレッド、スリッページを必要に応じて考慮する。
5. BUYとSELLを分けて集計できるようにする。
6. サンプル数を必ず表示する。
7. 過学習を避けるため、学習期間と未使用テスト期間を分けることを推奨する。
8. 最終的には実運用予定のMT5ブローカーの価格データでも再検証する。

---

## 7. 戦略研究ループ

```text
① ユーザーが狙いたい値動きを説明
        ↓
② GPTが仮説を数値条件へ変換
        ↓
③ 過去検証
        ↓
④ JSON生成
        ↓
⑤ HTMLビューアで目視確認
        ↓
⑥ ユーザーがフィードバック
   ・このEntryは違う
   ・この形は狙い通り
   ・戻りがもっと速いものだけ
   ・H1条件を追加したい
        ↓
⑦ GPTが戦略を修正
        ↓
⑧ 再検証
        ↺
```

最終判断は、**期待値が高いだけでなく、チャート上で狙っていた現象を正しく検出しているか**も確認する。

---

## 8. MT5実運用フェーズ

過去検証で戦略が完成した後に、別モジュールとしてMT5監視プログラムを作る。

### MT5側の役割

- 対象銘柄を24時間監視
- Strategyの固定条件をリアルタイム計算
- 条件成立時のみGPT APIを呼ぶ
- MT5注文を実行
- 注文 / 約定 / エラーをログ保存

### GPTの役割

GPTは条件成立後の**最終裁量**を担当する。

```text
固定条件成立
   ↓
GPT
├─ BUY
├─ SELL
└─ HOLD
```

GPTへ渡すデータは**判断時点まで**に限定する。

### GPTに任せないもの

資金管理は固定プログラムで管理し、GPTが自由変更できないようにする。

- 最大ロット
- 1トレード最大損失
- 1日最大損失
- 最大保有ポジション数
- SL必須条件
- 異常スプレッド時の停止
- 緊急停止

---

## 9. GitHub / GitHub Pages方針

対象リポジトリ：

```text
tgkfnfnfv9-stack/Gpt-Verification--software
```

既定ブランチ：

```text
main
```

GitHub Pagesではリポジトリ直下の `index.html` を公開ページとして使用する。

このプロジェクトでは `report.html` と同内容の `index.html` を用意する。

### 推奨リポジトリ構成

```text
Gpt-Verification--software/
├─ index.html
├─ report.html
├─ README.md
├─ GPT出力JSON_仕様.md
├─ GPT用_出力指示.md
├─ プロジェクト指示書.md
├─ Work_GitHub書き込み依頼書.md
└─ samples/
   └─ GPT出力データ_サンプル.json
```

### GitHub Pages設定

ファイルを書き込んだ後、GitHubで以下を設定する。

```text
Settings
  ↓
Pages
  ↓
Source: Deploy from a branch
  ↓
Branch: main
  ↓
Folder: /(root)
  ↓
Save
```

その後、GitHub Pagesの公開URLへアクセスして表示を確認する。

---

## 10. セキュリティルール

GitHubへ以下を絶対に保存しない。

- OpenAI API Key
- MT5ログインパスワード
- ブローカーAPI Key
- アクセストークン
- 秘密鍵
- 個人情報を含むログ

GitHub Pagesは公開リポジトリの場合、公開情報として扱う。

---

## 11. 現在の開発優先順位

### Phase 1：現在

汎用HTMLビューアを完成させる。

- 複数JSON対応
- 1件削除 / 全削除
- 複数銘柄
- 複数時間足
- 複数インジケータ
- 日本語UI
- チャート確認
- 検証指標表示

### Phase 2

ChatGPT → 検証 → JSON → HTMLの実データ運用を確立する。

### Phase 3

戦略完成後にMT5監視プログラムを作る。

### Phase 4

固定条件成立時のみGPT APIへ最終裁量を依頼し、固定リスク管理を通過した注文だけMT5へ送る。
