name: archived-disabled-temp-gbpjpy-h1-v8

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "This legacy workflow is permanently disabled."
          echo "It accessed periods reserved by the Phase 9 policy."
          exit 1
